# animal_classification
This project implements an Animal Classification system using a Convolutional Neural Network (CNN) built on MobileNetV2 (Transfer Learning). The system is designed to classify animal images across 15 categories (Bear, Bird, Cat, etc.).
